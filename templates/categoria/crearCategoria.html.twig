{% extends 'base.html.twig' %}

{% block title %}Crear Categoria{% endblock %}

{% block body %}

    <div class="container">
        <div class="col-md-8">
            {% for flash_message in app.session.flashbag.get('notice') %}
                <div class="alert alert-success">
                {{ flash_message }}
                </div>
            {% endfor %}
            <h1>Bienvenido a la landing Page de Crear Categoria</h1>

            <p>Si estás viendo esto, es que funciona.</p>

            {{ form_start(formCategoria) }}
            {{ form_widget(formCategoria) }}
            {{ form_end(formCategoria) }}
        </div>
    </div>

{% endblock %}
